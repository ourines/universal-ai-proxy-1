{
  "name": "universal-ai-proxy-1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "type-check": "tsc --noEmit",
    "lint": "echo 'Linting not configured yet'"
  },
  "dependencies": {
    "hono": "^4.8.5",
    "openai": "^4.67.3",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0",
    "@types/node": "^22.10.2",
    "@types/uuid": "^10.0.0"
  }
}